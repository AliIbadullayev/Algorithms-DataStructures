#include <iostream>
using namespace std;

int main() {
    int n, k;
    cin >> n >> k;
    int arr[n];
    int result;
    for (int i = 0; i < n ; i++) cin >> arr [i];
    for (int i = 0; i < n ; i++) cout << arr [i];
    for (int i = 0; i <= n - k ; i++){
        for (int j = 0; j<=n)
    }
    return 0;
}
